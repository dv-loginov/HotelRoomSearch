include ../icon-text/icon-text

mixin icon-text-list(args)
    if !args
        - args = {}
    .icon-text-list
        +icon-text({
            mod: "list",
            title: "Комфорт",
            subtitle: "Шумопоглощающие стены"
        })
        +icon-text({
            mod: "list",
            title: "Удобство",
            subtitle: "Окно в каждой из спален"
        })
