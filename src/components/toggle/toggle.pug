mixin toggle(args)
    .toggle-wrap
        label.toggle
            input.toggle__input( type="checkbox" name=args.name checked=!!args.checked )
            span.toggle-track
                span.toggle-indicator
                    span.checkMark
        span=args.text
